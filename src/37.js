// Example 1: Simple function definition
function add(x, y) {
    return x + y;
}

console.log(add(5, 3)); // Output: 8

// Example 2: Function that adds two numbers and returns the result
function sum(a, b) {
    return a + b;
}

console.log(sum(4, 6)); // Output: 10
